# Development + test dependencies
# Install with: pip install -r requirements-dev.txt
#
# Includes everything in requirements.txt plus tooling for
# linting, type-checking, testing, notebooks, and report export.

-r requirements.txt

# Testing
pytest>=7.4,<9.0
pytest-cov>=4.1,<6.0

# Lint + format
ruff>=0.4,<1.0

# Type checking
mypy>=1.8,<2.0

# Notebooks
jupyter>=1.0
notebook>=7.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0
pandas>=2.0,<3.0
ipykernel>=6.0

# Notebook validation in CI
nbformat>=5.9

# Optional: LangChain integration (uncomment to enable)
langchain>=0.3
langchain-core>=0.3
langchain-openai>=0.1
python-dotenv>=1.0
